<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.gowid.katok-watch</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/zsh</string>
    <string>-lc</string>
    <string>$HOME/.claude/scripts/katok_watch.sh</string>
  </array>

  <!-- 상주 루프. 죽으면 launchd 가 다시 띄운다.
       WatchPaths 는 카톡 샌드박스 컨테이너 경로에서 발동하지 않아 못 쓴다(katok_watch.sh 주석).

       🚨 전제 — katok 바이너리에 「전체 디스크 접근 권한」이 있어야 한다 (2026-08-11 부여).
       없으면 launchd 가 katok 을 부를 때마다 「다른 앱의 데이터에 접근」 승인창이 뜨고,
       2초 루프가 그걸 무한 팝업으로 만든다(당일 사고). 「앱 데이터」 승인만으로는 부족했다 —
       katok 이 adhoc 서명이라 TCC 가 신원을 고정하지 못해 매번 다시 물었다.

       확인:  sqlite3 "/Library/Application Support/com.apple.TCC/TCC.db" \
                "SELECT auth_value FROM access
                 WHERE service='kTCCServiceSystemPolicyAllFiles' AND client LIKE '%katok%';"
              → 2 면 허용.

       katok 을 재빌드하면 서명 해시가 바뀌어 권한이 깨진다. 재빌드 후에는 이 잡을 내리고
       시스템 설정에서 권한을 다시 준 뒤 올릴 것. -->
  <key>KeepAlive</key><true/>
  <key>RunAtLoad</key><true/>
  <key>ThrottleInterval</key><integer>10</integer>

  <key>StandardErrorPath</key><string>/tmp/katok-watch.err</string>
</dict>
</plist>
